<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('./sidebar') %>
    <div id="main">
      <h1><%= title %></h1>
      <% if (family_instruments.length) { %>
        <p>Please delete the following instruments before attempting to delete this family:</p>
        <ul>
        <% for (let instrument of family_instruments) { %>
            <li><%= instrument.type %> - <%= instrument.maker.company_name %></li>
            <% } %>
        </ul>
        <% } else { %>
            <p>Are you sure you want to delete this family?</p>
            <form method='POST' action=''>
                <input name='family_id' type='hidden' required='true' value='<%= family._id %>'>
                <button type='submit'>Yes, delete.</button>
            </form>
            <% } %>
    </div>
  </body>
</html>
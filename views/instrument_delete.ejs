<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('./sidebar') %>
    <div id="main">
      <h1><%= title %></h1>
      <% if (instrument_instances.length) { %>
        <p>Please delete the following instances before attempting to delete this instrument:</p>
        <ul>
        <% for (let instance of instrument_instances) { %>
            <li><%= instance.instrument.type %> - <%= instance.status %> - <%= instance.notes %></li>
            <% } %>
        </ul>
        <% } else { %>
            <p>Are you sure you want to delete this instrument?</p>
            <form method='POST' action=''>
                <input name='instrument_id' type='hidden' required='true' value='<%= instrument._id %>'>
                <button type='submit'>Yes, delete.</button>
            </form>
            <% } %>
    </div>
  </body>
</html>
<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('./sidebar') %>
    <div id="main">
      <h1><%= title %></h1>
      <% if (maker_instruments.length) { %>
        <p>Please delete the following instruments before attempting to delete this maker:</p>
        <ul>
        <% for (let instrument of maker_instruments) { %>
            <li><%= instrument.type %></li>
            <% } %>
        </ul>
        <% } else { %>
            <p>Are you sure you want to delete this maker?</p>
            <form method='POST' action=''>
                <input name='maker_id' type='hidden' required='true' value='<%= maker._id %>'>
                <button type='submit'>Yes, delete.</button>
            </form>
            <% } %>
    </div>
  </body>
</html>